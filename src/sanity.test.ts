import { describe, expect, it } from "vitest";

// Placeholder smoke test for the Phase 0.1 scaffold. Real coverage
// begins with lib/scoring/engine.ts in Phase 1.1 (see ROADMAP.md).
describe("scaffold sanity", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
